#!/usr/bin/env python3
"""BrowserOS Build System CLI executable."""

import sys
from pathlib import Path

# Add the cli directory to Python path
cli_path = Path(__file__).parent / "cli"
sys.path.insert(0, str(cli_path.parent))

from cli import app

if __name__ == "__main__":
    app()