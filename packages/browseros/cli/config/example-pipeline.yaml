# Example pipeline configuration
# Path to chromium source (can be overridden with --chromium-src)
paths:
  chromium_src: /path/to/chromium/src

pipelines:
  macos-debug:
    matrix:
      architectures: [x64]
    env:
      GN_FLAGS: build/config/gn/flags.macos.debug.gn
    steps:
      - clean
      - setup-env
      # - git-sync  # Not implemented yet
      # - patch-chromium
      # - patch-strings
      # - configure
      # - build
      # - package-mac